stages:
  - null_login

variables:
  API_URL: "https://dummy.restapiexample.com/api/v1/employee/$NULL_TOKEN"

null_login:
  stage: null_login
  image: ubuntu:latest
  before_script:
    - apt-get update
    - apt-get install -y openssl curl
  script:
    - RESPONSE=$(curl "$API_URL")
    - echo "$RESPONSE" > api_response.txt
    - echo "$encription_key"
    - openssl enc -aes-256-cbc -salt -in api_response.txt -out api_response.enc -pass file:$encription_key
  artifacts:
    paths:
      - api_response.enc
      - api_response2.enc